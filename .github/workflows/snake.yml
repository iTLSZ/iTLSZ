name: Generate Snake

on:
  schedule:
    # cada 12 horas
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
    - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate Snake SVG
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: iTLSZ
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#6366F1&color_dots=#e0e7ff,#c7d2fe,#818cf8,#4f46e5,#312e81
            dist/github-contribution-grid-snake-dark.svg?color_snake=#6366F1&color_dots=#1e1e2f,#312e81,#4f46e5,#818cf8,#c7d2fe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
